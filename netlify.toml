[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "22"
  # Public Procore endpoint URL variables are not secrets; omit these keys from scanner checks.
  SECRETS_SCAN_OMIT_KEYS = "PROCORE_API_URL,PROCORE_AUTH_URL"

